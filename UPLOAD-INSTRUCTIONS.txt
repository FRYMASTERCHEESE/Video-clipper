CLIPFREE AI — OAUTH DEMO BUGFIX

Replace ONLY these two files in FRYMASTERCHEESE/Video-clipper:
1. youtube.js
2. oauth-demo.html

Do NOT replace index.html or your other ClipFree files.

WHAT THIS FIXES
- OAuth success no longer gets falsely changed back to "not connected" just because analytics/videos/playlists had a loading problem.
- Top status changes immediately after Google grants access.
- Connected state hides the real Connect button and shows Disconnect.
- Disconnect updates the UI immediately and revokes the token in the background.
- Demo Connect/Disconnect buttons call the real ClipFree functions directly instead of clicking hidden DOM buttons.
- Demo buttons wait until youtube.js is fully loaded.
- Optional API failures no longer break the entire connection.
- Analytics/video/playlist loading is isolated so one failure does not destroy the session.
- Demo page shows a clear connection-state box.
- Demo nav is simplified for faster recording.
- Anchor targets have mobile scroll spacing so sticky headers do not cover sections.

EXPECTED DEMO FLOW
1 Privacy Policy
2 Connect YouTube
Top bar should change: Connecting → Connected — <channel>
3 Analytics
4 Upload Tools
5 Disconnect
Top bar should return to YouTube not connected

If Google OAuth itself returns an error, the page will show the actual error instead of pretending the user is disconnected after a successful authorization.
