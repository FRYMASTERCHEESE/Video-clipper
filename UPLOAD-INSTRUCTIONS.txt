CLIPFREE AI — PRIVATE GOOGLE OAUTH TEST ROUTE

UPLOAD THESE 3 FILES TO THE ROOT OF:
FRYMASTERCHEESE/Video-clipper

1. index.html
   Replace the existing public index.html.
   This removes the Demo link/panel from the public homepage.

2. youtube.js
   Replace the existing youtube.js.
   While verification is pending, Google OAuth token requests are blocked on the public homepage.
   OAuth remains enabled on oauth-demo.html.

3. oauth-demo.html
   ADD this as a NEW file.
   Do not rename it.

PRIVATE TEST URL AFTER GITHUB PAGES UPDATES:
https://frymastercheese.github.io/Video-clipper/oauth-demo.html

WHAT THE PRIVATE ROUTE DOES
- noindex / nofollow / noarchive
- not linked from the public ClipFree homepage
- hides unrelated sections during the recording
- keeps the real Privacy, OAuth, Analytics, Upload and Disconnect features
- uses the same real ClipFree code and OAuth scopes
- top status changes from not connected → connecting → connected

RECORDING ORDER
Privacy Policy → Connect YouTube → Google warning/consent → Analytics → Upload Tools → Disconnect

AFTER GOOGLE APPROVES OAUTH
The temporary public-route OAuth block should be removed so normal users can connect from the public site again.
